#!/usr/bin/make -f
export PYBUILD_NAME=mailbox-cleaner

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installman:
	dh_installman man/mailbox-cleaner.1

override_dh_appstream:
	appstreamcli validate --no-net \
		data/metainfo/cz.vitexsoftware.mailbox-cleaner.metainfo.xml
